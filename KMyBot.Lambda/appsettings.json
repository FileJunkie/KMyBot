{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Information"
    }
  },
  "BotConfiguration": {
    "BotToken": "${BOT_TOKEN}",
    "SecretToken": "${BOT_SECRET_TOKEN}"
  },
  "AuthConfiguration":
  {
    "RedirectUri": "${REDIRECT_URI}",
    "ClientId": "${DROPBOX_KEY}",
    "ClientSecret": "${DROPBOX_SECRET}"
  }
}